---
const dossier = [
  { k: 'Role',     v: 'Full-stack engineer · backend-leaning' },
  { k: 'Current',  v: 'Vue · Go' },
  { k: 'Learning', v: 'Rust' },
  { k: 'Also',     v: 'TypeScript / Node · C# / .NET' },
  { k: 'Location', v: 'Kyiv / Ukraine' },
  { k: 'Status',   v: 'Open for collab' },
];
---

<section id="about" class="section">
  <div class="container">
    <h2 class="section-title">About</h2>

    <div class="about-grid">
      <div class="about-copy">
        <p>
          Currently working with <strong>Vue</strong> on the front end and
          <strong>Go</strong> on the server, actively learning
          <strong>Rust</strong>. I like clean APIs, predictable systems, and
          codebases that prize clarity over cleverness.
        </p>
        <p>
          Comfortable picking up <strong>TypeScript / Node</strong> and
          <strong>C# / .NET</strong> when a project calls for them.
          Based in Kyiv, Ukraine.
        </p>
      </div>

      <aside class="dossier">
        <dl>
          {dossier.map((row) => (
            <>
              <dt>{row.k}</dt>
              <dd>{row.v}</dd>
            </>
          ))}
        </dl>
      </aside>
    </div>
  </div>
</section>

<style>
  .about-grid {
    display: grid;
    grid-template-columns: 1.3fr 1fr;
    gap: 2.5rem;
    align-items: start;
  }

  .about-copy p {
    max-width: 58ch;
    color: var(--text-dim);
    line-height: 1.75;
    margin: 0 0 1rem;
  }
  .about-copy strong {
    color: var(--accent);
    font-weight: 600;
  }

  .dossier {
    background: var(--card);
    border: 1px solid var(--line);
    border-radius: 16px;
    padding: 1.5rem 1.75rem;
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    box-shadow: var(--card-shadow);
  }
  .dossier dl {
    margin: 0;
    display: grid;
    grid-template-columns: max-content 1fr;
    column-gap: 1.5rem;
    row-gap: 0.7rem;
  }
  .dossier dt {
    color: var(--text-mute);
    font-size: 0.78rem;
    letter-spacing: 0.04em;
    font-weight: 500;
    padding-top: 0.1rem;
  }
  .dossier dd {
    margin: 0;
    color: var(--text);
    font-size: 0.92rem;
  }

  @media (max-width: 820px) {
    .about-grid { grid-template-columns: 1fr; gap: 1.75rem; }
  }
</style>
